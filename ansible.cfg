[defaults]
inventory = inventory/hosts.ini
# Group name `k3s-edge` contains a hyphen; Ansible warns about this by default.
# Setting `ignore` leaves the name as-is and suppresses the warning.
force_valid_group_names = ignore
